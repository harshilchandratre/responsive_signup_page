// const enteredPassword = document.getElementById('password');
// const passwordPattern = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[!@#$%^&*]).{8,}$/;
// const passwordToggler = document.getElementById('password-toggler'); // Assuming there's only one element with this ID.

// enteredPassword.addEventListener('input', () => {
//     const passwordValue = enteredPassword.value; // Get the value of the input.

//     if (passwordPattern.test(passwordValue)) {
//         passwordToggler.style.display = 'block';
//     } else {
//         passwordToggler.style.display = 'none';
//     }
// });
